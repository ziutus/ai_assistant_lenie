---
# Service do dostępu do PGVector
apiVersion: v1
kind: Service
metadata:
  name: lenie-ai-db
  namespace: lenie-ai-dev
spec:
  selector:
    app: lenie-ai-db
  ports:
  - port: 5432
    targetPort: 5432
    name: postgres
  type: ClusterIP  # Dostępny tylko wewnątrz klastra
