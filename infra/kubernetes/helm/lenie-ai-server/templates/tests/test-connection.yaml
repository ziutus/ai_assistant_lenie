apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "lenie-ai-server.fullname" . }}-test-connection"
  labels:
    {{- include "lenie-ai-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "lenie-ai-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
