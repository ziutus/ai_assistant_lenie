apiVersion: v1
kind: Service
metadata:
  name: {{ include "lenie-ai-server.fullname" . }}
  labels:
    {{- include "lenie-ai-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lenie-ai-server.selectorLabels" . | nindent 4 }}
