AWSTemplateFormatVersion: '2010-09-09'
Description: 'API Gateway for main application for Project Lenie'

# 2026-07-02: trimmed to /url_add only. The 8 app-server-db document-CRUD endpoints and
# 2 app-server-internet endpoints were removed along with those Lambdas (RDS decommission,
# Docker/NAS is the primary document UI). Full 11-endpoint version preserved in git:
#   git show e9e7e20:infra/aws/cloudformation/templates/api-gw-app.yaml
# Restoration guide: docs/aws-serverless-restoration.md

Parameters:
  ProjectCode:
    Type: String
    Default: lenie
  Environment:
    Type: String
    Default: dev
    AllowedValues:
      - dev
      # Post-MVP: add prod, qa as needed
    Description: Deployment environment

Resources:
  LenieApi:
    Type: 'AWS::ApiGateway::RestApi'
    DeletionPolicy: Retain
    UpdateReplacePolicy: Retain
    Properties:
      Name: lenie_split
      Tags:
        - Key: Environment
          Value: !Ref Environment
        - Key: Project
          Value: !Ref ProjectCode
        - Key: ManagedBy
          Value: CloudFormation
        - Key: StackName
          Value: !Ref 'AWS::StackName'
      EndpointConfiguration:
        Types:
          - REGIONAL
      Body:
        openapi: "3.0.1"
        info:
          title: "lenie_split"
          version: "2026-07-02T00:00:00Z"
        paths:
          /url_add:
            post:
              responses:
                "200":
                  description: "200 response"
                  content:
                    application/json:
                      schema:
                        $ref: "#/components/schemas/Empty"
              security:
              - api_key: []
              x-amazon-apigateway-integration:
                type: "aws_proxy"
                httpMethod: "POST"
                uri: !Sub "arn:aws:apigateway:${AWS::Region}:lambda:path/2015-03-31/functions/arn:aws:lambda:${AWS::Region}:${AWS::AccountId}:function:${ProjectCode}-${Environment}-url-add/invocations"
                responses:
                  default:
                    statusCode: "200"
                passthroughBehavior: "when_no_match"
                timeoutInMillis: 29000
                contentHandling: "CONVERT_TO_TEXT"
            options:
              responses:
                "200":
                  description: "200 response"
                  headers:
                    Access-Control-Allow-Origin:
                      schema:
                        type: "string"
                    Access-Control-Allow-Methods:
                      schema:
                        type: "string"
                    Access-Control-Allow-Headers:
                      schema:
                        type: "string"
                  content:
                    application/json:
                      schema:
                        $ref: "#/components/schemas/Empty"
              x-amazon-apigateway-integration:
                type: "mock"
                responses:
                  default:
                    statusCode: "200"
                    responseParameters:
                      method.response.header.Access-Control-Allow-Methods: "'DELETE,GET,HEAD,OPTIONS,PATCH,POST,PUT'"
                      method.response.header.Access-Control-Allow-Headers: "'Content-Type,Authorization,X-Amz-Date,X-Api-Key,X-Amz-Security-Token'"
                      method.response.header.Access-Control-Allow-Origin: "'*'"
                requestTemplates:
                  application/json: "{\"statusCode\": 200}"
                passthroughBehavior: "when_no_match"
        components:
          schemas:
            Empty:
              title: "Empty Schema"
              type: "object"
          securitySchemes:
            api_key:
              type: "apiKey"
              name: "x-api-key"
              in: "header"
  # Stage managed by separate ApiStage resource
  ApiStage:
    Type: 'AWS::ApiGateway::Stage'
    DeletionPolicy: Retain
    UpdateReplacePolicy: Retain
    Properties:
      RestApiId: !Ref LenieApi
      DeploymentId: !Ref ApiDeployment
      StageName: 'v1'
      Description: 'CF-managed v1 stage'
      TracingEnabled: true
      Tags:
        - Key: Environment
          Value: !Ref Environment
        - Key: Project
          Value: !Ref ProjectCode
        - Key: ManagedBy
          Value: CloudFormation
        - Key: StackName
          Value: !Ref 'AWS::StackName'
      MethodSettings:
        - HttpMethod: '*'
          ResourcePath: '/*'
          LoggingLevel: INFO
          MetricsEnabled: true
          # DataTraceEnabled logs full request/response bodies — review before enabling in production
          DataTraceEnabled: true

  # StageName kept on Deployment (CloudFormation does not support removing it from an existing resource)
  ApiDeployment:
    Type: 'AWS::ApiGateway::Deployment'
    DeletionPolicy: Retain
    UpdateReplacePolicy: Retain
    Properties:
      RestApiId: !Ref LenieApi
      StageName: 'v1'

  # SSM Parameter exports
  ApiGatewayIdParameter:
    Type: 'AWS::SSM::Parameter'
    Properties:
      Name: !Sub '/${ProjectCode}/${Environment}/apigateway/app/id'
      Type: String
      Value: !Ref LenieApi
      Description: 'API Gateway app REST API ID for Project Lenie'
      Tags:
        Environment: !Ref Environment
        Project: !Ref ProjectCode
        ManagedBy: CloudFormation
        StackName: !Ref 'AWS::StackName'

  ApiGatewayRootResourceIdParameter:
    Type: 'AWS::SSM::Parameter'
    Properties:
      Name: !Sub '/${ProjectCode}/${Environment}/apigateway/app/root-resource-id'
      Type: String
      Value: !GetAtt LenieApi.RootResourceId
      Description: 'API Gateway app root resource ID for Project Lenie'
      Tags:
        Environment: !Ref Environment
        Project: !Ref ProjectCode
        ManagedBy: CloudFormation
        StackName: !Ref 'AWS::StackName'

  ApiGatewayInvokeUrlParameter:
    Type: 'AWS::SSM::Parameter'
    Properties:
      Name: !Sub '/${ProjectCode}/${Environment}/apigateway/app/invoke-url'
      Type: String
      Value: !Sub 'https://${LenieApi}.execute-api.${AWS::Region}.amazonaws.com/v1'
      Description: 'API Gateway app invoke URL for Project Lenie'
      Tags:
        Environment: !Ref Environment
        Project: !Ref ProjectCode
        ManagedBy: CloudFormation
        StackName: !Ref 'AWS::StackName'

  UrlAddLambdaInvokePermission:
    Type: AWS::Lambda::Permission
    Properties:
      FunctionName: !Sub '${ProjectCode}-${Environment}-url-add'
      Action: lambda:InvokeFunction
      Principal: apigateway.amazonaws.com
      SourceArn: !Sub 'arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${LenieApi}/*/*/url_add'
