# Add URL App (React)

Minimal single-page React application for submitting URLs to the Lenie AI backend via `POST /url_add`. No routing, no document browsing — just a URL submission form.

See [CLAUDE.md](CLAUDE.md) for detailed documentation (directory structure, form fields, API communication, Docker build). For the full project architecture see the [root CLAUDE.md](../CLAUDE.md).

## Running

```bash
npm start        # Dev server (port 3000)
npm run build    # Production build
npm test         # Jest tests
```
